apiVersion: apps/v1
kind: Deployment

metadata:
  name: customer
  namespace: ecommerce

spec:
  replicas: 1

  selector:
    matchLabels:
      app: customer

  template:
    metadata:
      labels:
        app: customer

    spec:
      containers:
        - name: customer

          image: asia-south1-docker.pkg.dev/project-e4b58c33-b31e-44d9-a36/ecommerce-microservices/customer:5f973ac262e20fec967c67c4da6855c84eb2055d

          imagePullPolicy: IfNotPresent

          ports:
            - containerPort: 8001

          env:
            - name: SPRING_PROFILES_ACTIVE
              value: kube

            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://postgres:5432/customer

            - name: SPRING_DATASOURCE_USERNAME
              value: postgres

            - name: SPRING_DATASOURCE_PASSWORD
              value: password

            - name: SPRING_RABBITMQ_ADDRESSES
              value: rabbitmq:5672

            - name: SPRING_ZIPKIN_BASE_URL
              value: http://zipkin:9411

            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: http://eureka-server:8761/eureka

          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"

            limits:
              cpu: "500m"
              memory: "512Mi"
